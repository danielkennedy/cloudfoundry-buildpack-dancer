#!/usr/bin/env bash
# bin/release <build-dir>
BUILD_DIR=$1

cat << EOF
config_vars:
  PATH: '$BUILD_DIR/bin:$PATH'
  PERL5DIR: '$BUILD_DIR/lib/perl5'
default_process_types:
  web: perl bin/app.pl
EOF
