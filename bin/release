#!/usr/bin/env bash
# bin/release <build-dir>
cat << EOF
addons:
config_vars:
  PATH: runtime/bin:/usr/local/bin:/usr/bin:/bin
default_process_types:
  web: perl -Mlib=\$PWD/runtime/lib/perl5 bin/app.pl
EOF
